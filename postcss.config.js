module.exports = {
  plugins: [
    // postcss-smart-import might be useful later, bookmarking for now...
    // require('postcss-smart-import')({ /* ...options */ }),
    require('autoprefixer')({ /* ...options */ })
  ]
}
